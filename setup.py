from setuptools import setup, find_packages

setup(
    name='SSEChannels',
    version="0.0.1",
    author="Florent D'halluin",
    author_email='flal@melix.net',
    description=("EventSource for django, using channels"),
    license='BSD',
    packages= ['ssechannels'],
)
